行内 <b>HTML 标签</b> 渲染为实体，<i>斜体</i> 与 <code>code</code> 也是。

未知标签 <notreal>按原文保留</notreal> 为文本。

<div>未知块级 HTML 原样保留</div>

含 <br/> 的行。

嵌套 <b><i>加粗斜体</i></b> 与错位 <b>b <i>bi</b> i</i> 恢复。

未闭合 <b>只影响本块

<a href="https://example.com">链接标签</a> 与 <s>删除线</s>。

<pre><code class="language-python">print("hi")</code></pre>
